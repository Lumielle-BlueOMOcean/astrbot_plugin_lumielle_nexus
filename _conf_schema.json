{
  "operator_ids": {
    "type": "list",
    "description": "允许通过私聊控制群枢的额外 QQ 用户 ID",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "timezone": {
    "type": "string",
    "description": "任务时间解析使用的时区",
    "default": "Asia/Shanghai"
  },
  "scheduler_interval_seconds": {
    "type": "int",
    "description": "提醒 scheduler 检查间隔（秒）",
    "default": 15,
    "hint": "运行时会限制在 5 到 60 秒之间。"
  },
  "max_retry_count": {
    "type": "int",
    "description": "提醒发送失败后的最大重试次数",
    "default": 3
  },
  "collection_ack": {
    "type": "bool",
    "description": "群成员提交后是否回复简短确认",
    "default": true
  },
  "archive_max_message_chars": {
    "type": "int",
    "description": "单条群消息归档的最大文本字符数",
    "default": 4000,
    "hint": "运行时会限制在 256 到 20000 之间。"
  },
  "archive_retention_days": {
    "type": "int",
    "description": "群消息归档自动保留天数，0 表示不自动清理",
    "default": 90,
    "hint": "非 0 值运行时会限制在 7 到 3650 天之间。"
  },
  "moderation_enabled": {
    "type": "bool",
    "description": "是否开启单成员群管理；默认关闭，开启后仍需 moderator_ids 或 AstrBot Admin",
    "default": false
  },
  "moderator_ids": {
    "type": "list",
    "description": "允许执行单成员群管理的 QQ 用户 ID；不会自动继承 operator_ids",
    "default": [],
    "items": {
      "type": "string"
    }
  }
}
